# Production Environment Configuration
# API requests go through NGINX reverse proxy

# API Base URL - Use /api for NGINX proxy routing
VITE_API_BASE_URL=/api

# Environment identifier
VITE_ENVIRONMENT=production

# Node environment
NODE_ENV=production
